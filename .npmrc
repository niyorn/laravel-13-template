# Blocks install scripts from dependencies, which is where npm supply-chain attacks live.
# It also blocks our own `prepare`, so the git hooks need `npx lefthook install` by hand.
ignore-scripts=true
